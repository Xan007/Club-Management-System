---
import AdminLayout from '../../layouts/AdminLayout.astro'

export const prerender = false
---

<AdminLayout title="Espacios - Club del Meta" active="espacios">
  <section class="panel">
    <header class="panel__header">
      <div>
        <p class="eyebrow">Administración</p>
        <h1>Espacios</h1>
        <p class="muted">Gestiona los salones del club: edita información, imágenes, disposiciones y precios.</p>
      </div>
      <div class="actions">
        <button id="btnRefrescarEspacios" class="btn secondary">Refrescar</button>
        <button id="btnNuevoEspacio" class="btn primary">+ Nuevo Salón</button>
      </div>
    </header>

    <div class="grid">
      <section class="card">
        <div class="card__header">
          <div>
            <p class="eyebrow">Salones</p>
            <h2>Lista de espacios</h2>
          </div>
        </div>
        <div id="espaciosList" class="espacios-list">
          <div class="placeholder">Cargando espacios...</div>
        </div>
      </section>

    </div>
  </section>

  <!-- Modal de confirmación para eliminar -->
  <div id="confirmModal" class="modal-overlay hidden">
    <div class="modal-confirm">
      <div class="modal-confirm__icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="10"></circle>
          <line x1="15" y1="9" x2="9" y2="15"></line>
          <line x1="9" y1="9" x2="15" y2="15"></line>
        </svg>
      </div>
      <h3 class="modal-confirm__title">¿Eliminar espacio?</h3>
      <p class="modal-confirm__message">Esta acción no se puede deshacer. El espacio <strong id="espacioEliminarNombre"></strong> será eliminado permanentemente con todas sus configuraciones.</p>
      <div class="modal-confirm__actions">
        <button id="btnCancelarEliminar" class="btn ghost">Cancelar</button>
        <button id="btnConfirmarEliminar" class="btn danger">Eliminar</button>
      </div>
    </div>
  </div>

</AdminLayout>

<script>
import '@scripts/espacios/lista.ts'
</script>

<style>
  * { box-sizing: border-box; }
  body { overflow-x: hidden; max-width: 100vw; }
  .panel { padding: 2rem; max-width: 1200px; margin: 0 auto 4rem; width: 100%; box-sizing: border-box; }
  .panel__header { display: flex; justify-content: space-between; gap: 1rem; align-items: flex-start; max-width: 100%; overflow: hidden; }
  .eyebrow { text-transform: uppercase; letter-spacing: 0.08em; font-size: 0.8rem; color: #0a4ba5; margin: 0 0 0.3rem; }
  h1, h2, h3 { margin: 0; color: #0f172a; }
  h3 { font-size: 1.05rem; }
  .muted { color: #4b5563; margin: 0.4rem 0 0; max-width: 640px; }
  .actions { display: flex; gap: 0.5rem; align-items: center; }
  .inline-actions { display: flex; gap: 0.75rem; align-items: center; }
  .grid { display: grid; grid-template-columns: 1fr; gap: 1.2rem; margin-top: 1.5rem; max-width: 100%; }
  .card { background: #ffffff; border: 1px solid #e5e7eb; border-radius: 16px; padding: 1.25rem; box-shadow: 0 10px 30px rgba(15,23,42,0.06); max-width: 100%; box-sizing: border-box; }
  .card__header { display: flex; justify-content: space-between; gap: 1rem; align-items: center; flex-wrap: wrap; }
  .selector { display: flex; gap: 1rem; align-items: flex-end; flex-wrap: wrap; }
  label { font-size: 0.9rem; color: #4b5563; display: flex; flex-direction: column; gap: 0.25rem; }
  select, input, textarea { border: 1px solid #d1d5db; border-radius: 10px; padding: 0.55rem 0.75rem; font: inherit; background: #ffffff; color: #111827; }
  textarea { resize: vertical; }
  .btn { border: none; border-radius: 10px; padding: 0.65rem 1rem; font-weight: 600; cursor: pointer; font: inherit; transition: transform 0.1s ease, box-shadow 0.2s ease; text-decoration: none; display: inline-flex; align-items: center; justify-content: center; box-sizing: border-box; max-width: 100%; }
  .btn.primary { background: linear-gradient(135deg, #0a4ba5, #2563eb); color: #fff; box-shadow: 0 10px 20px rgba(37,99,235,0.2); }
  .btn.secondary { background: #eef2ff; color: #1d4ed8; border: 1px solid #dbeafe; }
  .btn.ghost { background: #ffffff; color: #0f172a; border: 1px solid #e5e7eb; }
  .btn.danger { background: linear-gradient(135deg, #dc2626, #b91c1c); color: #fff; box-shadow: 0 10px 20px rgba(220, 38, 38, 0.25); }
  .btn:hover { transform: translateY(-1px); }
  .btn.danger:hover { background: linear-gradient(135deg, #b91c1c, #991b1b); box-shadow: 0 12px 24px rgba(220, 38, 38, 0.3); transform: translateY(-2px); }
  .pill { display: inline-flex; align-items: center; gap: 0.3rem; padding: 0.3rem 0.75rem; border-radius: 999px; font-weight: 600; font-size: 0.85rem; border: 1px solid #e5e7eb; }
  .pill-active { background: #ecfdf3; color: #166534; border-color: #bbf7d0; }
  .pill-inactive { background: #fef2f2; color: #b91c1c; border-color: #fecdd3; }
  .pill-muted { background: #f8fafc; color: #6b7280; }
  .placeholder { padding: 1rem; color: #6b7280; text-align: center; border: 1px dashed #e5e7eb; border-radius: 12px; background: #f8fafc; }
  .form { display: grid; gap: 1rem; }
  .form-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 1rem; }
  .wide { grid-column: 1 / -1; }
  .subcard { border: 1px solid #e5e7eb; border-radius: 12px; padding: 1rem; background: #f8fafc; }
  .subcard__header { display: flex; justify-content: space-between; gap: 1rem; align-items: center; flex-wrap: wrap; }
  .add-config { display: flex; gap: 0.5rem; align-items: center; flex-wrap: wrap; }
  .config-list { display: grid; gap: 0.75rem; margin-top: 0.75rem; }
  :global(.config-card) { display: grid; grid-template-columns: 1fr auto auto; gap: 0.75rem; align-items: center; padding: 0.75rem; border: 1px solid #e5e7eb; border-radius: 10px; background: #ffffff; box-shadow: 0 6px 16px rgba(15,23,42,0.04); }
  :global(.config-card .capacidad) {
    width: 140px;
    text-align: center;
    border-radius: 12px;
    border: 1px solid #c7d2fe;
    background: linear-gradient(135deg, #f8fafc, #eef2ff);
    padding: 0.5rem 0.65rem;
    font-weight: 700;
    color: #0f172a;
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.8), 0 6px 12px rgba(59,130,246,0.08);
    transition: border-color 0.15s ease, box-shadow 0.2s ease;
  }
  :global(.config-card .capacidad:focus) { outline: none; border-color: #2563eb; box-shadow: inset 0 1px 0 rgba(255,255,255,0.9), 0 0 0 3px rgba(37,99,235,0.18); }
  :global(.config-card .capacidad::-webkit-outer-spin-button),
  :global(.config-card .capacidad::-webkit-inner-spin-button) { margin: 0; }
  :global(.config-card .capacidad[type='number']) { -moz-appearance: textfield; }
  .config-title { font-weight: 600; color: #0f172a; }
  .actions-end { display: flex; justify-content: flex-end; margin-top: 0.5rem; }
  :global(.link) {
    appearance: none;
    border: 1px solid #dbeafe;
    background: linear-gradient(135deg, #eef2ff, #e0e7ff);
    color: #1d4ed8;
    cursor: pointer;
    padding: 0.45rem 0.65rem;
    border-radius: 10px;
    font-weight: 700;
    font-size: 0.9rem;
    box-shadow: 0 6px 12px rgba(37,99,235,0.12);
    transition: transform 0.12s ease, box-shadow 0.2s ease, filter 0.2s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
  }
  :global(.link:hover) { transform: translateY(-1px); box-shadow: 0 10px 20px rgba(37,99,235,0.18); }
  :global(.link:focus) { outline: 2px solid #bfdbfe; outline-offset: 2px; }
  :global(.link.danger) {
    border-color: #fecdd3;
    background: linear-gradient(135deg, #fff1f2, #ffe4e6);
    color: #b91c1c;
    box-shadow: 0 6px 12px rgba(185,28,28,0.12);
  }
  .switch { display: inline-flex; align-items: center; gap: 0.35rem; font-size: 0.9rem; color: #4b5563; }
  .switch input { width: 18px; height: 18px; }
  .espacios-list { display: grid; gap: 0.75rem; margin-top: 0.75rem; }
  
  :global(.espacio-card) { 
    display: grid; 
    grid-template-columns: 120px 1fr auto; 
    gap: 1rem; 
    align-items: center; 
    padding: 1rem; 
    border: 1px solid #e5e7eb; 
    border-radius: 12px; 
    background: #ffffff; 
    box-shadow: 0 6px 16px rgba(15,23,42,0.04); 
    transition: box-shadow 0.2s ease;
    max-width: 100%;
    overflow: hidden;
    box-sizing: border-box;
  }
  
  :global(.espacio-card:hover) { 
    box-shadow: 0 10px 24px rgba(15,23,42,0.08); 
  }
  
  :global(.espacio-card img) {
    width: 120px;
    height: 90px;
    object-fit: cover;
    border-radius: 8px;
  }
  
  :global(.espacio-info) { 
    display: grid; 
    gap: 0.5rem;
    max-width: 100%;
    overflow: hidden;
  }
  
  :global(.espacio-tag) {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.25rem 0.6rem;
    border-radius: 999px;
    font-weight: 600;
    font-size: 0.8rem;
    width: fit-content;
  }
  
  :global(.destacado-tag) {
    background: #fef3c7;
    color: #92400e;
    border: 1px solid #fde68a;
  }
  
  :global(.espacio-nombre) { 
    font-weight: 700; 
    color: #0f172a; 
    font-size: 1.1rem; 
    margin: 0;
    word-break: break-word;
    overflow-wrap: break-word;
  }
  
  :global(.espacio-meta) { 
    display: flex; 
    gap: 0.75rem; 
    align-items: center; 
    flex-wrap: wrap; 
    color: #6b7280; 
    font-size: 0.9rem; 
  }
  
  :global(.espacio-actions) { 
    display: flex; 
    gap: 0.5rem; 
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    max-width: 100%;
    overflow: hidden;
  }
  
  :global(.espacio-actions .btn) { 
    font-size: 0.85rem; 
    padding: 0.5rem 0.85rem; 
    white-space: nowrap;
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    max-width: 100%;
    box-sizing: border-box;
  }
  
  :global(.espacio-actions .btn.secondary) {
    background: #eef2ff;
    color: #1d4ed8;
    border: 1px solid #dbeafe;
    box-shadow: 0 4px 10px rgba(37,99,235,0.1);
  }
  
  :global(.espacio-actions .btn.secondary:hover) {
    background: #dbeafe;
    box-shadow: 0 6px 14px rgba(37,99,235,0.15);
  }
  
  :global(.espacio-actions .btn.ghost) {
    background: #ffffff;
    color: #dc2626;
    border: 1px solid #fecaca;
    box-shadow: 0 4px 10px rgba(220,38,38,0.08);
  }
  
  :global(.espacio-actions .btn.ghost:hover) {
    background: #fef2f2;
    border-color: #dc2626;
    box-shadow: 0 6px 14px rgba(220,38,38,0.12);
  }
  /* Modal de confirmación */
  .hidden { display: none !important; }
  
  .modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(4px);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    opacity: 1;
    transition: opacity 0.3s ease;
  }
  
  .modal-overlay.hidden {
    opacity: 0;
    pointer-events: none;
  }
  
  .modal-confirm {
    background: white;
    border-radius: 20px;
    padding: 2rem;
    max-width: 440px;
    width: 90%;
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
    transform: scale(1);
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  
  .modal-overlay.hidden .modal-confirm {
    transform: scale(0.9);
  }
  
  .modal-confirm__icon {
    width: 80px;
    height: 80px;
    margin: 0 auto 1.5rem;
    background: linear-gradient(135deg, #fee2e2, #fecaca);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .modal-confirm__icon svg {
    color: #dc2626;
  }
  
  .modal-confirm__title {
    text-align: center;
    font-size: 1.5rem;
    color: #0f172a;
    margin: 0 0 0.75rem;
  }
  
  .modal-confirm__message {
    text-align: center;
    color: #64748b;
    line-height: 1.6;
    margin: 0 0 2rem;
  }
  
  .modal-confirm__message strong {
    color: #0f172a;
    font-weight: 600;
  }
  
  .modal-confirm__actions {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
  }
  
  .modal-confirm__actions .btn {
    flex: 1;
    justify-content: center;
  }

  /* Responsive Styles */
  @media (max-width: 1024px) {
    .panel { padding: 1.5rem; max-width: 100%; }
    .panel__header { flex-direction: column; align-items: stretch; }
    .actions { flex-wrap: wrap; width: 100%; }
    .actions .btn { flex: 1; min-width: 140px; justify-content: center; }
  }

  @media (max-width: 900px) {
    .panel__header { flex-direction: column; }
    .card__header { flex-direction: column; align-items: flex-start; }
    .config-card { grid-template-columns: 1fr; }
    .inline-actions { width: 100%; justify-content: space-between; flex-wrap: wrap; }
    :global(.espacio-card) { 
      grid-template-columns: 1fr; 
      gap: 1rem;
      overflow: hidden;
    }
    :global(.espacio-card img) { width: 100%; height: 200px; }
    :global(.espacio-info) {
      max-width: 100%;
      overflow: hidden;
    }
    :global(.espacio-nombre) {
      font-size: 1rem;
      word-break: break-word;
    }
  }

  @media (max-width: 768px) {
    .panel { padding: 1rem; width: 100%; max-width: 100%; overflow-x: hidden; }
    .card { padding: 1rem; max-width: 100%; overflow: hidden; }
    .panel__header h1 { font-size: 1.5rem; }
    .muted { font-size: 0.9rem; }
    .actions { flex-direction: column; gap: 0.75rem; }
    .actions .btn { width: 100%; min-width: auto; padding: 0.75rem 1rem; }
    :global(.espacio-actions) { 
      flex-direction: column; 
      width: 100%; 
      gap: 0.5rem;
    }
    :global(.espacio-actions .btn) { 
      width: 100%; 
      justify-content: center;
    }
    :global(.espacio-meta) {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }
    .modal-confirm {
      max-width: 90%;
      padding: 1.5rem;
    }
    .modal-confirm__actions {
      flex-direction: column;
    }
    .modal-confirm__actions .btn {
      width: 100%;
    }
    .modal-publicar {
      max-width: 90%;
      padding: 1.5rem;
    }
  }

  @media (max-width: 640px) {
    .panel { padding: 0.75rem; }
    .card { padding: 0.75rem; }
    .panel__header h1 { font-size: 1.25rem; }
    .eyebrow { font-size: 0.75rem; }
    :global(.espacio-card) {
      padding: 0.75rem;
    }
    :global(.espacio-card img) {
      height: 160px;
    }
    :global(.espacio-nombre) {
      font-size: 0.95rem;
    }
    :global(.espacio-meta) {
      font-size: 0.85rem;
    }
    .form-grid {
      gap: 0.75rem;
    }
  }

  @media (max-width: 480px) {
    .panel { padding: 0.5rem; }
    .card { padding: 0.5rem; }
    .panel__header h1 { font-size: 1.1rem; }
    .muted { font-size: 0.85rem; }
    .btn { font-size: 0.9rem; padding: 0.6rem 0.85rem; }
    :global(.espacio-card img) {
      height: 140px;
    }
    :global(.espacio-nombre) {
      font-size: 0.9rem;
    }
    .modal-confirm {
      padding: 1rem;
    }
    .modal-confirm__title {
      font-size: 1.25rem;
    }
    .modal-publicar {
      padding: 1rem;
    }
  }
</style>
