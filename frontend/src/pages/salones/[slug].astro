---
import BaseLayout from '../../layouts/BaseLayout.astro'
import Navbar from '../../components/Navbar.astro'
import { salonesData, getSalonBySlug } from '../../data/salones'

export function getStaticPaths() {
  return salonesData.map(salon => ({
    params: { slug: salon.slug },
    props: { salon }
  }))
}

const { slug } = Astro.params
const { salon } = Astro.props

if (!salon) {
  return Astro.redirect('/salones')
}
---

<BaseLayout title={`${salon.nombre} - Club del Meta`} hideNavbar fullWidth>
  <!-- Hero del Salón -->
  <section class="salon-hero">
    <div 
      class="salon-hero__background" 
      style={`background-image: linear-gradient(rgba(4, 14, 32, 0.6), rgba(4, 14, 32, 0.8)), url(${salon.imagenes[0]?.url || '/src/assets/millanura.jpg'})`}
    ></div>

    <div class="navbar-wrapper">
      <Navbar />
    </div>

    <div class="salon-hero__content">
      {salon.destacado && <span class="salon-hero__destacado">Salón Destacado</span>}
      <h1>{salon.nombre}</h1>
      <p class="salon-hero__subtitulo">{salon.subtitulo}</p>
    </div>
  </section>

  <!-- Contenido Principal -->
  <section class="salon-content-section">
    <div class="salon-content">
      <!-- Sidebar con información rápida -->
      <aside class="salon-sidebar">
        <a href="/salones" class="back-button">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="19" y1="12" x2="5" y2="12"></line>
            <polyline points="12 19 5 12 12 5"></polyline>
          </svg>
          Volver a Salones
        </a>

        <div class="salon-info-card">
          <h3>Información Rápida</h3>
          
          <div class="info-item">
            <div class="info-item__icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="9" cy="7" r="4"></circle>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
              </svg>
            </div>
            <div class="info-item__content">
              <span class="info-item__label">Capacidad</span>
              <span class="info-item__value">{salon.capacidad.minima} - {salon.capacidad.maxima} personas</span>
            </div>
          </div>

          <div class="info-item">
            <div class="info-item__icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
                <line x1="3" y1="9" x2="21" y2="9"></line>
                <line x1="9" y1="21" x2="9" y2="9"></line>
              </svg>
            </div>
            <div class="info-item__content">
              <span class="info-item__label">Dimensiones</span>
              <span class="info-item__value">{salon.dimensiones}</span>
            </div>
          </div>

          <div class="info-item">
            <div class="info-item__icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"></circle>
                <polyline points="12 6 12 12 16 14"></polyline>
              </svg>
            </div>
            <div class="info-item__content">
              <span class="info-item__label">Horario</span>
              <span class="info-item__value">{salon.horarioDisponible}</span>
            </div>
          </div>

          <div class="info-item info-item--price">
            <div class="info-item__icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="12" y1="1" x2="12" y2="23"></line>
                <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path>
              </svg>
            </div>
            <div class="info-item__content">
              <span class="info-item__label">Precio desde</span>
              <span class="info-item__value info-item__value--price">{salon.precioDesde}</span>
            </div>
          </div>
        </div>

        <a href={`/obtener-cotizacion?salon=${encodeURIComponent(salon.nombre)}`} class="cta-button">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
            <line x1="16" y1="2" x2="16" y2="6"></line>
            <line x1="8" y1="2" x2="8" y2="6"></line>
            <line x1="3" y1="10" x2="21" y2="10"></line>
          </svg>
          Solicitar Cotización
        </a>

        <p class="sidebar-updated">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
            <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
          </svg>
          Actualizado: {new Date(salon.fechaActualizacion).toLocaleDateString('es-CO', { year: 'numeric', month: 'long', day: 'numeric' })}
        </p>
      </aside>

      <!-- Contenido del Blog -->
      <main class="salon-main">
        <!-- Galería de Imágenes -->
        <section class="salon-gallery">
          <h2>Galería</h2>
          <div class="gallery-grid">
            {salon.imagenes.map((imagen, index) => (
              <div 
                class={`gallery-item ${index === 0 ? 'gallery-item--main' : ''}`}
                data-index={index}
                role="button"
                tabindex="0"
              >
                <img src={imagen.url} alt={imagen.alt} loading="lazy" />
                <div class="gallery-item__overlay">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <circle cx="11" cy="11" r="8"></circle>
                    <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                    <line x1="11" y1="8" x2="11" y2="14"></line>
                    <line x1="8" y1="11" x2="14" y2="11"></line>
                  </svg>
                </div>
              </div>
            ))}
          </div>
        </section>

        <!-- Descripción Completa -->
        <section class="salon-description">
          <h2>Sobre este espacio</h2>
          <div class="description-content" set:html={salon.descripcionCompleta}></div>
        </section>

        <!-- Características -->
        <section class="salon-features">
          <h2>Características</h2>
          <ul class="features-list">
            {salon.caracteristicas.map((caracteristica) => (
              <li>
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
                {caracteristica}
              </li>
            ))}
          </ul>
        </section>

        <!-- Servicios Incluidos -->
        <section class="salon-services">
          <h2>Servicios Incluidos</h2>
          <ul class="services-list">
            {salon.serviciosIncluidos.map((servicio) => (
              <li>
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                  <polyline points="22 4 12 14.01 9 11.01"></polyline>
                </svg>
                {servicio}
              </li>
            ))}
          </ul>
        </section>

        <!-- CTA Final -->
        <section class="salon-cta">
          <div class="cta-card">
            <h3>¿Listo para reservar {salon.nombre}?</h3>
            <p>Nuestro equipo está disponible para ayudarte a planificar tu evento perfecto.</p>
            <div class="cta-actions">
              <a href={`/obtener-cotizacion?salon=${encodeURIComponent(salon.nombre)}`} class="cta-primary">
                Solicitar Cotización
              </a>
              <a href="/contacto" class="cta-secondary">
                Contactar
              </a>
            </div>
          </div>
        </section>
      </main>
    </div>
  </section>

  <!-- Modal de Imagen -->
  <div class="image-modal" id="imageModal" role="dialog" aria-hidden="true">
    <button class="modal-close" aria-label="Cerrar">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="18" y1="6" x2="6" y2="18"></line>
        <line x1="6" y1="6" x2="18" y2="18"></line>
      </svg>
    </button>
    <img src="" alt="" class="modal-image" />
    <div class="modal-nav">
      <button class="modal-prev" aria-label="Anterior">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="15 18 9 12 15 6"></polyline>
        </svg>
      </button>
      <button class="modal-next" aria-label="Siguiente">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </button>
    </div>
  </div>
</BaseLayout>

<script define:vars={{ imagenes: salon.imagenes }}>
  // Lightbox functionality
  const modal = document.getElementById('imageModal');
  const modalImg = modal?.querySelector('.modal-image');
  const galleryItems = document.querySelectorAll('.gallery-item');
  let currentIndex = 0;

  function openModal(index) {
    currentIndex = index;
    if (modal && modalImg) {
      modalImg.src = imagenes[currentIndex].url;
      modalImg.alt = imagenes[currentIndex].alt;
      modal.classList.add('active');
      modal.setAttribute('aria-hidden', 'false');
      document.body.style.overflow = 'hidden';
    }
  }

  function closeModal() {
    if (modal) {
      modal.classList.remove('active');
      modal.setAttribute('aria-hidden', 'true');
      document.body.style.overflow = '';
    }
  }

  function navigate(direction) {
    currentIndex = (currentIndex + direction + imagenes.length) % imagenes.length;
    if (modalImg) {
      modalImg.src = imagenes[currentIndex].url;
      modalImg.alt = imagenes[currentIndex].alt;
    }
  }

  galleryItems.forEach((item, index) => {
    item.addEventListener('click', () => openModal(index));
    item.addEventListener('keypress', (e) => {
      if (e.key === 'Enter') openModal(index);
    });
  });

  modal?.querySelector('.modal-close')?.addEventListener('click', closeModal);
  modal?.querySelector('.modal-prev')?.addEventListener('click', () => navigate(-1));
  modal?.querySelector('.modal-next')?.addEventListener('click', () => navigate(1));

  modal?.addEventListener('click', (e) => {
    if (e.target === modal) closeModal();
  });

  document.addEventListener('keydown', (e) => {
    if (!modal?.classList.contains('active')) return;
    if (e.key === 'Escape') closeModal();
    if (e.key === 'ArrowLeft') navigate(-1);
    if (e.key === 'ArrowRight') navigate(1);
  });
</script>

<style>
  /* Hero Section */
  .salon-hero {
    position: relative;
    min-height: 70vh;
    display: flex;
    flex-direction: column;
    color: #ffffff;
    overflow: hidden;
    margin: 0;
    width: 100%;
  }

  .salon-hero__background {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    width: 100%;
  }

  .navbar-wrapper {
    position: relative;
    z-index: 10;
  }

  .navbar-wrapper :global(.navbar) {
    background: transparent !important;
    box-shadow: none !important;
    position: relative !important;
  }

  .navbar-wrapper :global(.navbar__link) {
    color: #ffffff !important;
  }

  .navbar-wrapper :global(.navbar__link:hover) {
    color: #60a5fa !important;
  }

  .navbar-wrapper :global(.navbar__toggle span) {
    background: #ffffff !important;
  }

  .salon-hero__content {
    position: relative;
    text-align: center;
    max-width: 900px;
    margin: auto;
    padding: 3rem 2rem;
    z-index: 1;
  }

  .salon-hero__destacado {
    display: inline-block;
    background: var(--color-accent);
    color: white;
    padding: 0.5rem 1.25rem;
    border-radius: 30px;
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 1rem;
  }

  .salon-hero h1 {
    color: white;
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    margin: 0 0 1rem;
    font-weight: 700;
    letter-spacing: 0.02em;
  }

  .salon-hero__subtitulo {
    color: rgba(255, 255, 255, 0.95);
    font-size: clamp(1rem, 2vw, 1.25rem);
    line-height: 1.7;
    margin: 0 auto;
    max-width: 600px;
  }

  /* Content Section */
  .salon-content-section {
    background: #ffffff;
    padding: 3rem 2rem;
  }

  /* Content Layout */
  .salon-content {
    display: grid;
    grid-template-columns: 320px 1fr;
    gap: 3rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  /* Sidebar */
  .salon-sidebar {
    position: sticky;
    top: 100px;
    height: fit-content;
  }

  .salon-sidebar .back-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-primary);
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    padding: 0.5rem 1rem;
    border-radius: 8px;
    transition: all 0.2s ease;
    background: #f1f5f9;
    margin-bottom: 1.5rem;
    white-space: nowrap;
  }

  .salon-sidebar .back-button:hover {
    background: #e2e8f0;
    transform: translateX(-4px);
  }

  .salon-sidebar .back-button svg {
    transition: transform 0.2s ease;
  }

  .salon-sidebar .back-button:hover svg {
    transform: translateX(-2px);
  }

  .salon-info-card {
    background: white;
    border-radius: 16px;
    padding: 1.5rem;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    margin-bottom: 1.5rem;
  }

  .salon-info-card h3 {
    font-size: 1.1rem;
    color: #1e293b;
    margin: 0 0 1.25rem;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid #e2e8f0;
  }

  .info-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 0.75rem 0;
    border-bottom: 1px solid #f1f5f9;
  }

  .info-item:last-child {
    border-bottom: none;
  }

  .info-item__icon {
    width: 40px;
    height: 40px;
    background: #f1f5f9;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-primary);
    flex-shrink: 0;
  }

  .info-item--price .info-item__icon {
    background: #fef3c7;
    color: #d97706;
  }

  .info-item__content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .info-item__label {
    font-size: 0.8rem;
    color: #64748b;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .info-item__value {
    font-size: 0.95rem;
    color: #1e293b;
    font-weight: 500;
  }

  .info-item__value--price {
    color: #d97706;
    font-weight: 700;
    font-size: 1.1rem;
  }

  .cta-button {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    width: 100%;
    background: var(--color-primary);
    color: white;
    padding: 1rem 1.5rem;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.3s ease;
    box-shadow: 0 4px 14px rgba(10, 75, 165, 0.3);
    box-sizing: border-box;
  }

  .cta-button:hover {
    background: #083d8a;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(10, 75, 165, 0.4);
  }

  .sidebar-updated {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: #94a3b8;
    font-size: 0.8rem;
    margin-top: 1rem;
    text-align: center;
    justify-content: center;
  }

  /* Main Content */
  .salon-main section {
    margin-bottom: 3rem;
  }

  .salon-main h2 {
    font-size: 1.5rem;
    color: #1e293b;
    margin: 0 0 1.25rem;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid #e2e8f0;
  }

  /* Gallery */
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .gallery-item {
    aspect-ratio: 4/3;
    border-radius: 12px;
    overflow: hidden;
    cursor: pointer;
    position: relative;
  }

  .gallery-item--main {
    grid-column: span 2;
    grid-row: span 2;
  }

  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .gallery-item:hover img {
    transform: scale(1.05);
  }

  .gallery-item__overlay {
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.3s ease;
    color: white;
  }

  .gallery-item:hover .gallery-item__overlay {
    opacity: 1;
  }

  /* Description */
  .description-content {
    line-height: 1.8;
    color: #475569;
  }

  .description-content :global(h3) {
    font-size: 1.2rem;
    color: #1e293b;
    margin: 1.5rem 0 0.75rem;
  }

  .description-content :global(p) {
    margin: 0 0 1rem;
  }

  /* Features & Services */
  .features-list,
  .services-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
  }

  .features-list li,
  .services-list li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: #f8fafc;
    border-radius: 8px;
    color: #475569;
    font-size: 0.95rem;
  }

  .features-list svg {
    color: var(--color-primary);
    flex-shrink: 0;
  }

  .services-list svg {
    color: #10b981;
    flex-shrink: 0;
  }

  /* CTA Card */
  .cta-card {
    background: linear-gradient(135deg, #0a4ba5 0%, #083d8a 100%);
    border-radius: 16px;
    padding: 2.5rem;
    text-align: center;
    color: white;
  }

  .cta-card h3 {
    font-size: 1.5rem;
    margin: 0 0 0.75rem;
  }

  .cta-card p {
    opacity: 0.9;
    margin: 0 0 1.5rem;
  }

  .cta-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }

  .cta-primary,
  .cta-secondary {
    padding: 0.875rem 2rem;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .cta-primary {
    background: white;
    color: var(--color-primary);
  }

  .cta-primary:hover {
    background: #f1f5f9;
    transform: translateY(-2px);
  }

  .cta-secondary {
    background: transparent;
    color: white;
    border: 2px solid rgba(255,255,255,0.5);
  }

  .cta-secondary:hover {
    background: rgba(255,255,255,0.1);
    border-color: white;
  }

  /* Image Modal */
  .image-modal {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.95);
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
  }

  .image-modal.active {
    opacity: 1;
    visibility: visible;
  }

  .modal-image {
    max-width: 90vw;
    max-height: 85vh;
    object-fit: contain;
    border-radius: 8px;
  }

  .modal-close {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    background: rgba(255,255,255,0.1);
    border: none;
    color: white;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s;
  }

  .modal-close:hover {
    background: rgba(255,255,255,0.2);
  }

  .modal-nav {
    position: absolute;
    bottom: 2rem;
    display: flex;
    gap: 1rem;
  }

  .modal-prev,
  .modal-next {
    background: rgba(255,255,255,0.1);
    border: none;
    color: white;
    width: 56px;
    height: 56px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s;
  }

  .modal-prev:hover,
  .modal-next:hover {
    background: rgba(255,255,255,0.2);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .salon-content {
      grid-template-columns: 1fr;
    }

    .salon-sidebar {
      position: static;
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 1rem;
      align-items: start;
    }

    .salon-info-card {
      margin-bottom: 0;
    }

    .cta-button {
      align-self: start;
    }

    .sidebar-updated {
      grid-column: 1 / -1;
    }
  }

  @media (max-width: 968px) {
    .salon-hero .navbar-wrapper :global(.navbar__links) {
      background: rgba(10, 75, 165, 0.98) !important;
    }

    .salon-hero {
      min-height: 60vh;
    }

    .salon-content-section {
      padding: 3rem 1.5rem;
    }
  }

  @media (max-width: 640px) {
    .salon-hero h1 {
      font-size: 2rem;
    }

    .salon-hero p {
      font-size: 1rem;
    }

    .salon-sidebar {
      grid-template-columns: 1fr;
    }

    .salon-sidebar .back-button {
      width: 100%;
      justify-content: center;
      font-size: 0.8rem;
      padding: 0.4rem 0.75rem;
      gap: 0.35rem;
    }

    .salon-sidebar .back-button svg {
      width: 14px;
      height: 14px;
    }

    .salon-info-card {
      padding: 1.5rem;
    }

    .cta-button {
      width: 100%;
    }

    .gallery-grid {
      grid-template-columns: 1fr;
    }

    .gallery-item--main {
      grid-column: span 1;
      grid-row: span 1;
    }

    .features-list,
    .services-list {
      grid-template-columns: 1fr;
    }

    .cta-actions {
      flex-direction: column;
    }

    .cta-card {
      padding: 1.5rem;
    }
  }
</style>
